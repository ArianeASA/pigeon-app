package main

const (
	smtpHost     = "SMTP_HOST"
	smtpPort     = "SMTP_PORT"
	smtpUser     = "SMTP_USER"
	smtpPass     = "SMTP_PASS"
	secretKey    = "SECRET_ENCRYPTION_KEY"
	headMetadata = "HEAD_METADATA"
)
